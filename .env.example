# Environment Variables Template
# Copy this file to .env and fill in your values

# Microlink API Key (Optional)
# Get your API key from https://microlink.io
# Used for fetching Open Graph data (thumbnails and metadata)
# The app works without this using the free tier, but Pro keys provide better rate limits
MICROLINK_API_KEY=your_api_key_here